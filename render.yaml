services:
  - type: web
    name: travel-by-petal-bot
    runtime: node
    rootDir: whatsapp-bot
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 20
